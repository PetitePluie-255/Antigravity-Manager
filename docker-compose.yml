services:
  antigravity-manager:
    build:
      context: .
      network: host
    network_mode: host
    volumes:
      - antigravity-data:/data
    environment:
      - PORT=3000
      - DATA_DIR=/data
    restart: unless-stopped

volumes:
  antigravity-data:
