services:
  antigravity-manager:
    build: .
    network_mode: host
    volumes:
      - antigravity-data:/data
    environment:
      - NODE_ENV=production
      - PORT=3000
      - DATA_DIR=/home/Antigravity/data
    restart: unless-stopped

volumes:
  antigravity-data:
